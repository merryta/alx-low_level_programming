#include "holberton.h"

int _isalpha(int c);

if (c = 'a' && c = 'z' || c = 'A' && c = 'Z') {
    return (1);
}else{
    return (0);
 }
}
int main(void)
{
    int r;

    r = _isalpha(c);
    _putchar(r + '0');
    _putchar('\n');
    return (0);
}
