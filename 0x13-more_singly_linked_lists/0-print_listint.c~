#include "lists.h"
/**
 *print_listlint - print all the element of listint_t list
 *@h: header of the list
 *
 *Return: node_number of the nodes
 */
size_t print_listint(const listint_t *h)
{
  int node_num = 0;

  while (h)
    printf("%i\n", h->n), node_num++, h = h->next;

  return (node_num);
}
